{
  "name": "@oriana/shared-layer",
  "version": "1.0.0",
  "description": "Shared Lambda Layer for Oriana Order Tracking API",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "node esbuild.config.js",
    "build:tsc": "tsc",
    "watch": "tsc -w",
    "prisma:generate": "prisma generate",
    "postinstall": "prisma generate",
    "postinstall:prod": "npm prune --production"
  },
  "dependencies": {
    "@prisma/client": "^5.7.0",
    "@aws-sdk/client-secrets-manager": "^3.450.0",
    "reflect-metadata": "^0.2.2",
    "inversify": "^6.0.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/aws-lambda": "^8.10.130",
    "prisma": "^5.7.0",
    "typescript": "^5.3.2",
    "esbuild": "^0.19.8"
  }
}
