# Deployment Workflow for Oriana Order Tracking
#
# This workflow deploys the application to AWS based on the selected environment.
# It builds the API, UI, and deploys via CDK.
#
# Trigger: Manual dispatch with environment selection
#
# Required GitHub Secrets:
#   - AWS_ACCESS_KEY_ID: AWS access key with deployment permissions
#   - AWS_SECRET_ACCESS_KEY: AWS secret access key
#   - AWS_REGION: AWS region (e.g., ap-south-1)

name: 🚀 Deploy

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy'
        required: true
        type: choice
        options:
          - dev
          - qa
          - prod
      deploy_type:
        description: 'What to deploy'
        required: true
        type: choice
        options:
          - full        # API + UI
          - api-only    # API only (faster)
      skip_ui_build:
        description: 'Skip UI build (use existing)'
        required: false
        type: boolean
        default: false

# Prevent concurrent deployments to the same environment
concurrency:
  group: deploy-${{ github.event.inputs.environment }}
  cancel-in-progress: false

env:
  NODE_VERSION: '22'
  AWS_REGION: ${{ secrets.AWS_REGION || 'ap-south-1' }}

jobs:
  deploy:
    name: Deploy to ${{ github.event.inputs.environment }}
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: |
            api/package-lock.json
            cdk/package-lock.json
            ui/package-lock.json

      - name: 🔑 Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: 📦 Install API dependencies
        working-directory: api
        run: npm ci

      - name: 📦 Install CDK dependencies
        working-directory: cdk
        run: npm ci

      - name: 🔨 Build API
        working-directory: api
        run: npm run build:all

      - name: 📦 Install UI dependencies
        if: github.event.inputs.deploy_type == 'full' && github.event.inputs.skip_ui_build == 'false'
        working-directory: ui
        run: npm ci

      - name: 🔨 Build UI
        if: github.event.inputs.deploy_type == 'full' && github.event.inputs.skip_ui_build == 'false'
        working-directory: ui
        run: npm run build
        env:
          CI: false  # Ignore warnings as errors

      - name: 🚀 Deploy to AWS (Full)
        if: github.event.inputs.deploy_type == 'full'
        working-directory: cdk
        run: |
          echo "🚀 Deploying API + UI to ${{ github.event.inputs.environment }}..."
          npx cdk deploy ApiStack-${{ github.event.inputs.environment }} --require-approval never

      - name: 🚀 Deploy to AWS (API Only)
        if: github.event.inputs.deploy_type == 'api-only'
        working-directory: cdk
        run: |
          echo "🚀 Deploying API only to ${{ github.event.inputs.environment }}..."
          npx cdk deploy ApiStack-${{ github.event.inputs.environment }} --require-approval never

      - name: 📋 Deployment Summary
        run: |
          echo "## 🎉 Deployment Complete!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Setting | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|---------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Environment | \`${{ github.event.inputs.environment }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| Deploy Type | \`${{ github.event.inputs.deploy_type }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| Branch | \`${{ github.ref_name }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| Commit | \`${{ github.sha }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| Triggered by | @${{ github.actor }} |" >> $GITHUB_STEP_SUMMARY

  # Production deployment requires approval
  prod-approval:
    name: Production Approval
    if: github.event.inputs.environment == 'prod'
    runs-on: ubuntu-latest
    environment: production-approval
    steps:
      - name: ⚠️ Production Deployment Notice
        run: |
          echo "⚠️ This is a PRODUCTION deployment!"
          echo "Please review the changes carefully before approving."

